---
phase: 01-verified-transaction-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - server.js
  - app/index.html
  - test/transaction.test.js
autonomous: true
requirements:
  - ARCH-02
  - ARCH-04
  - SAFE-02
  - SAFE-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SAFE-03 explicit edge: the persistent badge and every destructive confirmation identify mounted OP-Z versus local fixture, the active operation is visible, and native controls are disabled while work runs."
    - "SAFE-03 explicit edge: mounted-device outcomes include eject/reconnect/refresh instructions; local-fixture outcomes say no OP-Z was changed and instruct refresh/recovery as applicable."
    - "Verified archives and `verified:false` partial/failed diagnostics render in separate regions, and no unverified item has an enabled restore action."
    - "Safe direct UAT against `/Volumes/OP-Z` proves a mounted-slot archive changes only the laptop library and leaves the source SHA-256 unchanged."
  artifacts:
    - path: "app/index.html"
      provides: "Accessible source badge, active-operation state, disabled mutation controls, complete confirmations/results, and segregated draft diagnostics"
      contains: "aria-live"
    - path: "server.js"
      provides: "Sanitized source-specific result and guidance payloads consumed by the UI"
      contains: "guidance"
    - path: "test/transaction.test.js"
      provides: "Static UI/API contract checks plus the complete Phase 1 regression suite"
  key_links:
    - from: "server.js /api/state"
      to: "app/index.html render()"
      via: "STATE.source, STATE.mutation, and STATE.drafts drive persistent status and controls"
      pattern: "STATE\.(source|mutation|drafts)"
    - from: "app/index.html runMutation"
      to: "app/index.html mutation buttons"
      via: "try/finally toggles native disabled state around the shared api() call"
      pattern: "disabled"
    - from: "server.js archive response"
      to: "app/index.html toast/status"
      via: "verified/source/guidance payload supplies explicit outcome wording"
      pattern: "guidance"
  prohibitions:
    - statement: "MUST NOT describe a local-fixture operation as changing the OP-Z, or a mounted-device operation as local-only."
      status: "unresolved"
    - statement: "MUST NOT render a restore control for any partial, failed, legacy-unverified, or evidence-mismatched library item."
      status: "unresolved"
---

<objective>
Make the guarded transaction state, source identity, failure diagnostics, and recovery instructions unmistakable in the existing browser UI, then prove the read-only/archive path against the mounted OP-Z.

Purpose: The server safety boundary is only trustworthy to the user when intent, active work, verification status, and the next safe physical action are visible.
Output: Complete browser feedback and automated plus safe mounted-device acceptance evidence.
</objective>

## Phase Goal

**As Dougal, I want archive and restore operations to pin one OP-Z source and publish results only after verification, so that I can manage slots without losing recoverable song data.**

<execution_context>
@/Users/dougalhanson/.codex/gsd-core/workflows/execute-plan.md
@/Users/dougalhanson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verified-transaction-foundation/01-CONTEXT.md
@.planning/phases/01-verified-transaction-foundation/01-RESEARCH.md
@.planning/phases/01-verified-transaction-foundation/01-PATTERNS.md
@.planning/phases/01-verified-transaction-foundation/01-VALIDATION.md
@.planning/phases/01-verified-transaction-foundation/01-02-SUMMARY.md
@server.js
@app/index.html

<interfaces>
Server state contract from Plans 01-02: `/api/state` returns `source`, `mutation`, `drafts`, and verified library items; mutation success/failure returns stable `verified`, `source`, `guidance`, and recovery fields without paths.
Browser contract: keep native `button.disabled`, `confirm()`, `prompt()`, the existing source badge, `toast()`, and `api()`; add one `runMutation` wrapper rather than per-button busy flags.
Hardware contract: `/Volumes/OP-Z` is available for read-only identity/parse checks and an archive-to-laptop request; no restore, swap, clear, sample-pack write, forced disconnect, eject, or mount replacement is authorized in this plan.
</interfaces>
</context>

## Artifacts this phase produces

- `app/index.html`: `runMutation`, `sourceGuidance`, semantic mutation-button markers, accessible status output, and separate unverified-draft rendering.
- `server.js`: final sanitized `source`/`guidance`/recovery result shapes used by the browser.
- `test/transaction.test.js`: complete Phase 1 UI contract and filesystem/API regression checks.
- A verified Phase 1 UAT archive in `library/` created from slot 1 on `/Volumes/OP-Z`; it is runtime user data and must not be committed.

## Decision traceability

This plan implements D-05, D-09, D-10, D-11, D-12, and D-14 as defined in `SKELETON.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Show one source and one active operation across all mutation controls</name>
  <reversibility rating="reversible">The busy/status presentation is contained in the existing single-page UI and consumes stable server facts.</reversibility>
  <files>server.js, app/index.html, test/transaction.test.js</files>
  <read_first>server.js, app/index.html, test/transaction.test.js, .planning/phases/01-verified-transaction-foundation/01-CONTEXT.md, .planning/phases/01-verified-transaction-foundation/01-PATTERNS.md</read_first>
  <behavior>
    - Source badge: mounted OP-Z and local fixture remain persistently and visually distinct, and active operation/source/slot appears beside them.
    - Busy state: starting any browser mutation immediately disables all mutation controls; `try/finally` restores them on success and thrown error; server-reported active state survives refresh/cross-tab use.
    - Intent: destructive confirmations state operation, source, slot/current song where applicable, and whether device data may change.
    - Result: success/failure names the source and supplies the applicable eject/reconnect/refresh/recovery action through an accessible status region.
  </behavior>
  <action>Write static HTML/API contract assertions first. In `app/index.html`, mark every state-changing control with one semantic `data-mutation` attribute and add one `runMutation(operation, fn)` wrapper that sets a browser-local busy value, renders native `disabled` state immediately, awaits the existing `api()` path, and clears in `finally`; server `STATE.mutation` remains authoritative after reload and across tabs per D-11. Extend the existing source badge rather than adding a component: mounted OP-Z, local `opzdisk/`, no source, and active operation/source/slot must remain visible and use text as well as color. Centralize `sourceGuidance(source, outcome)` only if the server did not provide guidance, and make mounted results mention eject/reconnect/refresh while fixture results explicitly say no OP-Z changed. Update backup, restore, swap, clear, instrument move/remove/import/snapshot, and pack-download interactions to use `runMutation`; expand their existing native confirmations per D-09/D-10 without changing the operations they invoke. Give the persistent status/toast region `role="status"` and `aria-live="polite"`, preserve focus-visible styles, and use actual `button.disabled` plus one disabled CSS rule per D-14. In `server.js`, finalize the sanitized result fields needed by this rendering without returning absolute paths.</action>
  <verify>
    <automated>node --test test/transaction.test.js --test-name-pattern="source status UI|mutation controls|result guidance" &amp;&amp; node --check server.js</automated>
  </verify>
  <acceptance_criteria>
    - Static UI assertions find one shared `runMutation`, semantic mutation markers, native disabled assignment in `try/finally`, and an aria-live status region.
    - The badge text distinguishes mounted OP-Z, local fixture, no source, and active operation without relying only on color.
    - Archive/restore/swap confirmations contain source and slot/current song context; other device mutations contain source/target and device-change intent.
    - Mounted results contain eject/reconnect/refresh guidance; fixture results state that no OP-Z changed.
    - Errors restore controls and keep the stable server guidance visible long enough to act on it.
  </acceptance_criteria>
  <done>Dougal can always tell which source is targeted, what is running, which controls are unavailable, and what safe action follows the result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Segregate unverified drafts and prove a mounted archive leaves the OP-Z unchanged</name>
  <precondition>`/Volumes/OP-Z/projects/project01.opz` exists and is readable.</precondition>
  <reversibility rating="reversible">Draft rendering and the retained UAT archive are additive; the verified archive can be removed later through normal library management without changing device data.</reversibility>
  <files>app/index.html, test/transaction.test.js</files>
  <read_first>app/index.html, test/transaction.test.js, .planning/phases/01-verified-transaction-foundation/01-VALIDATION.md, .planning/phases/01-verified-transaction-foundation/01-CONTEXT.md</read_first>
  <behavior>
    - Draft UI: `STATE.drafts` renders in a separate labelled diagnostic region with `verified:false`, source/slot/time/stable error information, and no load/restore control.
    - Verified UI: only server-verified library items render the restore selector/button; legacy or evidence-mismatched items are labelled unverified and non-restorable.
    - Mounted UAT: state identifies `/Volumes/OP-Z` as a device, an API archive of slot 1 produces matching stored evidence, and source SHA-256 before/after is identical.
    - Safety fence: UAT performs no request to restore, swap, clear, instrument mutation, or pack download and does not simulate disappearance by ejecting/unplugging hardware.
  </behavior>
  <action>Start with UI contract tests for separate verified and unverified sample state. Update `renderLibrary()` per D-05 so verified shelf items and `STATE.drafts` use distinct headings/containers; failed/partial/legacy-unverified items must carry unmistakable text and no restore selector or load button. Keep error strings escaped with the existing `esc()` helper and expose only server-sanitized fields. Add one opt-in `node:test` case named `mounted API archive UAT`, skipped unless `OPZ_HARDWARE_UAT=1`, so the exact hardware acceptance is repeatable without another script. The case must record SHA-256 and byte length of `/Volumes/OP-Z/projects/project01.opz`; start the exported server on an ephemeral loopback port with `OPZ_ROOT=/Volumes/OP-Z` and browser opening disabled; GET `/api/state` and require `source.device === true`; POST JSON plus the custom mutation header to `/api/backup` for slot 1 with a clearly labelled Phase 1 UAT name and `deep:false`; locate only the returned server-controlled bundle name, compare its `song.opz` byte-for-byte with the captured device file, independently parse it, and confirm evidence SHA-256/length; finally recompute the device-file SHA-256 and require equality, close the server in cleanup, and retain the verified archive as uncommitted user data. Run the ordinary suite before setting the opt-in variable. Do not send any other mutation, do not use screen control, and do not disconnect/eject the mount.</action>
  <verify>
    <automated>node --test test/transaction.test.js &amp;&amp; node --check server.js &amp;&amp; OPZ_HARDWARE_UAT=1 node --test test/transaction.test.js --test-name-pattern="mounted API archive UAT"</automated>
  </verify>
  <acceptance_criteria>
    - UI fixture assertions prove no `verified:false` item creates a restore selector/button and verified items still do.
    - The full `node --test` suite and `node --check server.js` pass before mounted UAT.
    - `/api/state` reports mounted device source and the API-created UAT archive's stored bytes, independent parse, SHA-256, and length all match slot 1.
    - The mounted slot's before/after SHA-256 is identical and no file below `/Volumes/OP-Z` is written by the UAT.
    - The retained UAT archive is not added to git.
  </acceptance_criteria>
  <done>Unverified artifacts are visibly diagnostic-only, and direct filesystem/API evidence proves the mounted archive path preserves the OP-Z source bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server state/result → browser DOM | Sanitized source, operation, draft, and guidance facts control user intent and restore eligibility. |
| Browser controls → localhost API | Stale/cross-tab state cannot be trusted to enforce serialization or validation. |
| Mounted OP-Z → laptop archive UAT | Real removable hardware is read while the laptop library is written; any target confusion could affect user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | mounted UAT source | high | mitigate | Read/hash the explicit mounted file before and after; API uses pinned source and UAT sends no device-writing route. |
| T-01-02 | Tampering / Repudiation | verified/draft rendering | high | mitigate | Server evidence determines eligibility; drafts use a separate region and never receive restore controls. |
| T-01-03 | Tampering | DOM-derived identifiers | high | mitigate | UI values remain advisory; server validators and contained path derivation from Plan 02 stay authoritative. |
| T-01-04 | Tampering / Denial of Service | browser busy state | high | mitigate | `runMutation` gives immediate disabling while server active state/global guard rejects cross-tab overlap. |
| T-01-05 | Spoofing / Tampering | UI POST funnel | medium | mitigate | All mutations keep JSON/custom-header/Origin-Fetch-Metadata enforcement from Plan 02. |
| T-01-06 | Information Disclosure | draft/status DOM | medium | mitigate | Escape displayed text and render only stable code/source/slot/time/guidance fields, never paths or raw errors. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Capture one source, validate intent, preserve data, publish only after verification | 01-01, 01-02, 01-03 | COVERED | Tracer proves publication; expansions close bypasses and visibility. |
| REQ | ARCH-01 | Capture source bytes/device identity once | 01-01, 01-02 | COVERED | Captured Buffer and explicit source object feed every transactional helper. |
| REQ | ARCH-02 | Reread, byte-compare, reparse, SHA-256/length evidence | 01-01, 01-03 | COVERED | Automated temp test plus mounted archive UAT. |
| REQ | ARCH-04 | Source unchanged on capture/verification failure | 01-01, 01-03 | COVERED | Failure injection and before/after hardware hash. |
| REQ | SAFE-01 | Stop without fixture fallback on mount change | 01-01, 01-02 | COVERED | Direct pinned identity revalidation; resolver counter remains one. |
| REQ | SAFE-02 | Reject invalid slots/IDs/paths/concurrency | 01-02 | COVERED | Positive validators, containment, and one 409 guard. |
| REQ | SAFE-03 | Source visibility and physical guidance | 01-03 | COVERED | Persistent badge, confirmations, status, and outcome instructions. |
| CONTEXT | D-01 | One global reject-not-queue mutation | 01-01, 01-02 | COVERED | Awaited `withMutation` and route inventory. |
| CONTEXT | D-02 | Capture source Buffer once | 01-01 | COVERED | Archive writes only the captured Buffer. |
| CONTEXT | D-03 | Bind kind/root/filesystem/project identity | 01-01, 01-02 | COVERED | Plain captured object plus explicit helper parameters. |
| CONTEXT | D-04 | Stop on mount change, no fallback | 01-01, 01-02 | COVERED | Direct root/project checks. |
| CONTEXT | D-05 | Drafts separate and restore-ineligible | 01-01, 01-02, 01-03 | COVERED | Server scan eligibility plus separate UI. |
| CONTEXT | D-06 | Hidden stage then atomic rename | 01-01 | COVERED | Same-parent filesystem publication. |
| CONTEXT | D-07 | SHA-256/length/equality/parse | 01-01 | COVERED | Evidence from reread stored bytes. |
| CONTEXT | D-08 | Retained failure, source untouched | 01-01 | COVERED | `.failed` diagnostics and source hash assertions. |
| CONTEXT | D-09 | Persistent source badge/confirmation | 01-01, 01-03 | COVERED | Existing badge/dialog patterns extended. |
| CONTEXT | D-10 | Confirm operation/source/slot/song/device change | 01-01, 01-03 | COVERED | Native confirmation text. |
| CONTEXT | D-11 | Disable/show/reject while active | 01-01, 01-02, 01-03 | COVERED | Server guard plus browser status. |
| CONTEXT | D-12 | Explicit result and guidance | 01-01, 01-02, 01-03 | COVERED | Sanitized server fields rendered accessibly. |
| CONTEXT | D-13 | Small safe draft naming/retention | 01-01 | COVERED | `mkdtemp` and one hidden failed container. |
| CONTEXT | D-14 | Reuse badge/dialog/toast patterns | 01-01, 01-03 | COVERED | No UI framework/component layer. |
| RESEARCH | — | Keep CommonJS composition root and Node built-ins; add no package/build step | 01-01, 01-02, 01-03 | COVERED | Three existing files plus one test file. |
| RESEARCH | — | One shared async-capable guard and explicit captured source passage | 01-01, 01-02 | COVERED | Shared helpers, no route locks. |
| RESEARCH | — | Hidden stage → stored-byte verification → atomic publish; hidden failure retention | 01-01 | COVERED | Ordered tracer and negative tests. |
| RESEARCH | — | JSON/custom header plus Origin/Fetch Metadata and safe errors | 01-01, 01-02 | COVERED | Built-in localhost request boundary. |
| RESEARCH | — | `node:test` temp-fixture validation and safe mounted UAT | 01-01, 01-02, 01-03 | COVERED | One suite and one read/archive-only hardware check. |
| PATTERN | — | `server.js` composition root, `app/index.html` co-location, `parser.js` authoritative | 01-01, 01-02, 01-03 | COVERED | No new production module/parser. |
| PATTERN | — | One `test/transaction.test.js` integration file | 01-01, 01-02, 01-03 | COVERED | All safety checks stay in the built-in runner. |
| VALIDATION | — | Per-task quick suite; per-wave `node --test` + `node --check`; hardware-safe final check | 01-01, 01-02, 01-03 | COVERED | Every task has a runnable automated gate under 60 seconds. |

Excluded without gap: Phase 2 archive-manifest/shelf/manual-clear work; Phase 3 complete restore guarantees; split and automatic-clear phases; recorded post-milestone refinements. CONTEXT contains no Deferred Ideas.

<verification>
- `node --test` and `node --check server.js` pass before mounted UAT.
- Static UI contract tests prove semantic disabled controls, accessible status, source-aware text, and absent restore actions on every unverified item.
- Safe mounted UAT uses only GET state and POST backup, preserves the device file SHA-256, and verifies the returned laptop archive independently.
- Source audit has no MISSING row and every phase requirement appears in plan frontmatter.
</verification>

<success_criteria>
- Users see mounted/local source identity, active operation, disabled controls, explicit result, and applicable physical guidance.
- Unverified/failed artifacts are unmistakable, segregated, and non-restorable.
- The full fixture suite is green and the mounted archive check leaves slot 1 byte-identical.
- No screen control or device-writing UAT occurs.
</success_criteria>

<output>
Create `.planning/phases/01-verified-transaction-foundation/01-03-SUMMARY.md` when done.
</output>
