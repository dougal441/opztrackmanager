---
phase: 05-validated-split-half-archives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [parser.js, server.js, app/index.html, test/transaction.test.js]
autonomous: false
requirements: [SPLT-04, SPLT-05]
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A confirmed half can be synthesized deterministically from its parent without modifying parent bytes, with only its selected patterns retained and invalid chain references repaired per D-01 and D-02."
    - "The synthesized archive is reread, reparsed, and checked against retained-pattern fixtures before publication; failed checks never produce a restore-eligible archive."
    - "A synthesized archive remains visibly restore-ineligible until a recorded sacrificial-device eject, reconnect, rejection, playback, and recovery result exists per D-03 and D-04."
    - "With no OP-Z mounted, local tests pass and no hardware acceptance or restore eligibility is inferred per D-05."
  artifacts:
    - "parser.js exposes the existing layout constants needed for safe pattern/chain manipulation."
    - "server.js provides a confirmed-intent-bound half synthesis/archive path and derives restore eligibility from recorded acceptance evidence."
    - "app/index.html exposes half archive status and the explicit hardware-gated restore affordance."
    - "test/transaction.test.js covers deterministic bytes, chain repair, parse/retained-pattern checks, source immutability, and pending hardware gating."
  key_links:
    - "meta.splits[parentHash] → current parent-byte/hash validation → deterministic synthesis → verified archive manifest"
    - "synthesized manifest acceptance evidence → archive classifier/restore guard → UI eligibility"
    - "fixture and mounted acceptance evidence → no fallback from local success to device approval"
---

<objective>
Deliver safe independent archives for explicitly confirmed split halves, while keeping restore locked behind a recorded sacrificial-device format acceptance.

Purpose: Satisfy SPLT-04 and SPLT-05 without rewriting the parent project or treating a locally parseable binary as hardware-compatible.
Output: Deterministic synthesis/archive API, truthful archive-shelf status and restore gate, local integration checks, and a direct hardware acceptance checkpoint.
</objective>

<execution_context>
@/Users/dougalhanson/.codex/gsd-core/workflows/execute-plan.md
@/Users/dougalhanson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-split-review-confirmed-intent/04-CONTEXT.md
@.planning/phases/04-split-review-confirmed-intent/04-01-SUMMARY.md
@.planning/phases/05-validated-split-half-archives/05-CONTEXT.md
@parser.js
@server.js
@app/index.html
@test/transaction.test.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one confirmed half through deterministic synthesis and verified archive</name>
  <files>parser.js, server.js, app/index.html, test/transaction.test.js</files>
  <behavior>
    - Test 1: A confirmed parent hash and one exact half membership produce stable synthesized bytes, preserve the parent header/selected pattern regions, clear omitted pattern payloads, and leave parent bytes/hash unchanged.
    - Test 2: Every chain in the synthesized project contains only retained pattern indexes, invalid references are repaired deterministically, and the output reparses with exactly the selected retained patterns.
    - Test 3: Stored synthesized bytes are reread and checked against the synthesis result and parser/fixture evidence before publication; malformed, stale, or failed evidence returns a non-eligible diagnostic.
  </behavior>
  <action>Implement one end-to-end half-archive path per D-01 and D-02. Bind the request to the current confirmed `meta.splits[parentHash]`, revalidate the parent source bytes and exact membership, copy the parent buffer, preserve required header and selected pattern regions, clear omitted pattern regions, and repair chain entries without allowing references outside the selected set (retain valid chain structure where possible and remove empty/invalid entries deterministically). Reread and parse the synthesized bytes, compare retained-pattern parser/fixture facts exactly, and publish through the existing verified bundle/manifest machinery with parent provenance and half membership. Add the slot/split-review action and archive-shelf status using existing UI patterns; never write the source project. Use existing validators, atomic writers, and mutation serialization at the request boundary. Cite D-01 and D-02 in implementation comments or route documentation where the binary rules are non-obvious.</action>
  <verify>
    <automated>node --test test/transaction.test.js &amp;&amp; node --check parser.js &amp;&amp; node --check server.js</automated>
  </verify>
  <done>A user can explicitly archive either confirmed half into a verified project bundle; bytes are deterministic, parent bytes remain identical, all chain references are valid for the half, and local synthesis/reparse/retained-pattern tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the recorded sacrificial-device restore gate</name>
  <files>server.js, app/index.html, test/transaction.test.js</files>
  <behavior>
    - Test 1: A synthesized archive without a valid acceptance record is shown as retained but restore-ineligible, and restore requests fail closed.
    - Test 2: A bounded acceptance record is accepted only when it records eject, reconnect, rejection, playback, and recovery outcomes for the exact synthesized format/archive; unrelated or partial records do not unlock restore.
    - Test 3: The direct mounted acceptance test requires a real device and is skipped with an explicit pending result when no OP-Z is mounted; fixture success alone never changes eligibility.
  </behavior>
  <action>Extend archive classification and restore validation per D-03 and D-04 so synthesized manifests carry sanitized, versioned acceptance evidence and remain `restoreEligible: false` until all five required device outcomes match the synthesized format/provenance. Expose the pending/accepted reason in the archive shelf and require the existing explicit restore confirmation path to honor the derived gate. Add a direct-filesystem/API mounted UAT that performs eject, reconnect, rejection, playback, and recovery checks against a sacrificial slot, restores the original project on cleanup, and never substitutes `opzdisk/`; when no device is present, keep the result skipped/pending per D-05. Preserve all existing source-token, backup-before-mutation, path, and request validation protections. Do not add dependencies or browser-control tooling.</action>
  <verify>
    <automated>node --test test/transaction.test.js &amp;&amp; node --check server.js</automated>
    <human-check>With a sacrificial OP-Z in Content Mode, run `OPZ_HARDWARE_UAT=1 node --test test/transaction.test.js` and record the exact synthesized archive acceptance only after eject, reconnect, device rejection, playback, and original-project recovery all pass. Without a mounted OP-Z, confirm the UAT remains skipped/pending and synthesized archives remain restore-ineligible.</human-check>
  </verify>
  <done>Local verification is green with hardware eligibility pending when no device is mounted; only a complete recorded acceptance for the exact synthesized format unlocks restore, and failed/partial hardware checks remain safely ineligible with recovery guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → local API | Split names, parent hash, membership, archive id, and acceptance data are untrusted request input. |
| Source project → synthesized archive | Device/local bytes cross into a new stored bundle and must remain parent-bound and verified. |
| Hardware acceptance → restore eligibility | Device observations must be exact, bounded evidence rather than a client-controlled boolean. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering | `/api/split/archive` and parent bytes | high | mitigate | Revalidate parent SHA-256, confirmed exact membership, source identity, and immutable source before synthesis; atomically publish only reread/reparsed bytes. |
| T-05-02 | Tampering | Synthesized chain table | high | mitigate | Rewrite references through bounded selected-pattern membership and reject malformed output before archive publication. |
| T-05-03 | Elevation of privilege | Restore eligibility/acceptance record | critical | mitigate | Derive eligibility server-side from exact archive provenance and all five sanitized hardware outcomes; ignore client eligibility flags. |
| T-05-04 | Denial of service | Large/malformed synthesis and acceptance payloads | medium | mitigate | Reuse bounded request validators, fixed 16-pattern/32-entry format limits, and existing mutation serialization. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install is planned; no new dependency crosses the supply-chain boundary. |
</threat_model>

<source_audit>
| Source | ID | Feature / constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Deterministic confirmed half archives safe to restore only after hardware acceptance | 05-01 | COVERED |
| REQ | SPLT-04 | Deterministic synthesis, parent preservation, chain repair, reparse and retained-pattern checks | 05-01 | COVERED |
| REQ | SPLT-05 | Restore only after recorded sacrificial-device acceptance | 05-01 | COVERED |
| CONTEXT | D-01 | Exact confirmed membership drives synthesis | 05-01 | COVERED |
| CONTEXT | D-02 | Preserve selected bytes, clear omitted bytes, repair chains, reparse | 05-01 | COVERED |
| CONTEXT | D-03 | No restore eligibility without acceptance evidence | 05-01 | COVERED |
| CONTEXT | D-04 | Eject/reconnect/rejection/playback/recovery sequence | 05-01 | COVERED |
| CONTEXT | D-05 | No-device local completion with pending eligibility | 05-01 | COVERED |
| RESEARCH | — | No phase RESEARCH.md; existing stdlib/parser/server patterns are sufficient | 05-01 | COVERED |
</source_audit>

<verification>
Run `node --test test/transaction.test.js`, `node --check parser.js`, and `node --check server.js`. Confirm no parent project bytes change during local synthesis. With hardware unavailable, confirm the direct UAT is explicitly skipped/pending and no synthesized archive is restore-eligible; with hardware available, complete the blocking sacrificial-device acceptance check before enabling any restore eligibility.
</verification>

<success_criteria>
SPLT-04 and SPLT-05 pass through deterministic, reparsed, retained-pattern-checked half archives with repaired chains, immutable parent provenance, and a server-enforced five-step sacrificial-device gate. Local implementation and tests finish safely without an OP-Z; hardware absence leaves eligibility pending.
</success_criteria>

<output>
Create `.planning/phases/05-validated-split-half-archives/05-01-SUMMARY.md` when done.
</output>
