---
phase: 02-verified-archive-shelf-manual-freeing
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - server.js
  - app/index.html
  - test/transaction.test.js
autonomous: true
requirements:
  - ARCH-05
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-10: Archive Shelf is a top-level view beside Songs and Instruments, and verified records are deterministically newest-first with name, tags, archived matrix, provenance, creation time, snippet, completeness, and verification facts."
    - "D-11/D-17/D-18: Each compact row uses the smallest browser-native disclosure; expanded evidence derives its matrix from archived parser output and exposes project, snapshot, portability, provenance, and action facts without stored presentation markup."
    - "D-12: Diagnostics are a distinct landmark/list and never receive restore, manual-free, or target-slot controls."
    - "UI empty: verified shelf and diagnostics each render their own documented empty state; one empty collection never hides the other."
    - "UI loading: initial shelf load, archive creation, preflight-ready affordances, and refresh states expose aria-busy/live text and static or reduced-motion-safe skeletons."
    - "UI error: shelf failures render the documented retained-archive error and refresh action; no failure removes an archive or silently empties the view."
    - "UI populated: one or many verified archives use the same accessible disclosure structure, complete at-a-glance evidence, and newest-first order."
    - "UI partial: project-only supported records show verified/project-only separation, while legacy, malformed, incomplete, and evidence-mismatched records stay action-free diagnostics."
    - "UI overflow: critical text wraps and only the per-file evidence table may scroll horizontally; the page never gains sideways scrolling."
    - "UI zero-one-many: verified and diagnostic counts use deterministic singular/plural copy and require no pagination."
    - "UI long-text: names, tags, notes, archive-relative paths, hashes, and source labels wrap without ellipsis or tooltip-only meaning."
    - "Manifest-derived Unicode is escaped for its exact HTML/attribute context and preserved as readable text; color or iconography is never the only status cue."
  artifacts:
    - path: "server.js"
      provides: "Sanitized newest-first shelf and diagnostic projections backed by the Plan 01 classifier"
    - path: "app/index.html"
      provides: "One first-class Archive Shelf renderer, compact sidebar summary, accessible native disclosures, and all eight resolved UI states"
    - path: "test/transaction.test.js"
      provides: "Server/UI regressions for order, counts, matrices, empty/error/loading/overflow states, escaping, semantics, and diagnostic action absence"
  key_links:
    - from: "server.js scanLibrary()"
      to: "app/index.html renderArchives()"
      via: "`/api/state` returns classifier-derived shelf records and sanitized diagnostics in deterministic order"
      pattern: "scanLibrary|renderArchives|/api/state"
    - from: "server.js archived parse output"
      to: "app/index.html matrixSvg()"
      via: "archive patterns feed the existing matrix renderer; no SVG or HTML is stored in the manifest"
      pattern: "patterns|matrixSvg"
    - from: "app/index.html archive action"
      to: "Archive Shelf summary"
      via: "successful complete archive reloads state, selects the shelf tab, and focuses the new native summary"
      pattern: "backup|setTab|archive shelf"
---

<objective>
Deliver ARCH-05 as one first-class, evidence-driven Archive Shelf using the server classifier established in Plan 01.

Purpose: Let Dougal inspect trustworthy archive evidence and unmistakable diagnostics without duplicating archive rendering or inventing client-side truth.
Output: Newest-first shelf data, a browser-native Archive Shelf tab, compact Songs-sidebar counts/link, full evidence disclosures, and automated coverage of every approved UI state.
</objective>

## Phase Goal

**As a** Dougal, **I want to** create and inspect a complete verified archive before following guided on-device manual clearing, **so that** I can free an OP-Z slot without losing the complete song.

<execution_context>
@/Users/dougalhanson/.codex/gsd-core/workflows/execute-plan.md
@/Users/dougalhanson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-CONTEXT.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-RESEARCH.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-PATTERNS.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-UI-SPEC.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-VALIDATION.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-01-SUMMARY.md
@server.js
@app/index.html
@test/transaction.test.js

<interfaces>
Consume Plan 01's strict classifier through `scanLibrary()`; do not add a second shelf reader. Reuse `/api/state`, `load()`, `render()`, `setTab(t)`, `renderLibrary()`, `matrixSvg(s, w)`, `esc(s)`, `attr(s)`, `toast(msg)`, `api(path, body)`, `runMutation(operation, callback)`, and `backup(slot)`. Keep markup, CSS, and JavaScript co-located in `app/index.html`.
</interfaces>
</context>

<artifacts_this_plan_produces>
- A sanitized, newest-first Archive Shelf projection in `/api/state`.
- One `archive shelf` tab/view and `renderArchives()` implementation.
- Compact verified/diagnostic counts plus one shelf link in the Songs sidebar.
- Native archive/diagnostic disclosures and all approved empty, loading, error, populated, partial, overflow, count, and long-text behaviors.
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shape deterministic shelf and diagnostic data from the shared classifier</name>
  <files>server.js, test/transaction.test.js</files>
  <read_first>
    - `server.js`: Plan 01 classifier, `scanLibrary()`, `scanDrafts()`, `/api/state`, `publicSource()`, and public error serialization.
    - `test/transaction.test.js`: state response, library segregation, published corruption, metadata sanitization, and parser-backed archive tests.
    - `02-CONTEXT.md`: First-Class Archive Shelf decisions D-09 through D-12 and D-18.
    - `02-UI-SPEC.md`: Information Architecture, Compact verified archive row, Expanded evidence, Archive Diagnostics, and UI Considerations.
  </read_first>
  <behavior>
    - Verified shelf records and diagnostics remain independent arrays/collections with independent zero/one/many counts.
    - Verified records sort descending by validated manifest creation time, with a deterministic bundle-identifier tie-breaker.
    - Each verified response contains sanitized metadata, parsed archive patterns/tempo/chains, project evidence, snippet evidence/status, whole-grid counts/totals/per-track facts derived from current evidence, source slot/kind/label, and separate verified/complete facts.
    - Diagnostics expose only fallback display identifier, stable category/reason, safe creation/source facts when valid, and sanitized evidence; no action flags exist.
    - A project-only supported archive remains in the verified shelf with explicit incomplete/project-only status, while malformed/incomplete/legacy/tampered records remain diagnostics.
  </behavior>
  <action>
Extend the Plan 01 classifier's public projection and `/api/state` result for D-09 through D-12 without introducing a second scanner. Sort verified archive records newest-first by the validated ISO creation time and use bundle ID as a stable tie-breaker. Derive counts, totals, per-track sample-pack facts, tempo/chains/patterns, and matrix-ready parser data from current stored files; do not persist presentation markup or derived counters in the manifest. Return independently countable verified and diagnostic collections with safe source/provenance facts. Preserve project-only records as verified but incomplete and keep unsupported, malformed, legacy, failed, partial, and tampered material in diagnostics. Add tests first for zero/one/many records, identical-time tie-breaking, newest-first order, archived parser data, Unicode preservation, project-only separation, sanitized diagnostic reasons, and absence of action flags on diagnostics.
  </action>
  <acceptance_criteria>
    - [ ] ARCH-05 empty/count edge is resolved: verified and diagnostic collections have independent empty states/counts, correct singular/plural inputs, deterministic one/many behavior, and newest-first order.
    - [ ] The response includes every at-a-glance and expanded evidence field required by D-10/D-11, derived from current stored bytes and parser output.
    - [ ] Diagnostic/unverified records never carry restore, manual-free, target-slot, or generic action eligibility.
    - [ ] Public shelf JSON contains bounded Unicode data and sanitized relative evidence only; it exposes no absolute source/library path, credential, stack, or raw internal error.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern='shelf data|archive diagnostic|newest first|archive count|project only' test/transaction.test.js</automated>
  </verify>
  <done>`/api/state` provides one deterministic classifier-backed data source for verified shelf rows and separate action-free diagnostics, including all evidence needed by the approved UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render one accessible Archive Shelf and all eight approved states</name>
  <files>app/index.html, test/transaction.test.js</files>
  <read_first>
    - `app/index.html`: CSS tokens and focus rules, tab markup, `load()`, `setTab()`, `render()`, `matrixSvg()`, `renderSlots()`, `backup()`, `renderLibrary()`, `esc()`, `attr()`, toast/live status, and responsive media queries.
    - `test/transaction.test.js`: static UI segregation, hostile metadata escaping, browser `node:vm` behavior, source/status accessibility, and mutation reset tests.
    - `02-UI-SPEC.md`: all sections, especially exact copy, tab/disclosure semantics, focus, responsive behavior, reduced motion, and the eight UI Considerations.
    - `02-PATTERNS.md`: `app/index.html` analog and one-renderer prohibition.
  </read_first>
  <behavior>
    - Top navigation is a native-behaving tablist with Songs, Archive Shelf, and Instruments; roving focus supports Left/Right/Home/End and Enter/Space activation.
    - Songs sidebar renders verified/diagnostic counts and one `open archive shelf` control, not archive cards.
    - Verified archives use one `<details><summary>` renderer with all compact fields and evidence groups; diagnostics use a separate landmark/list and never render restore or manual-free actions.
    - Initial loading shows three aria-busy shelf skeletons; verified/diagnostic empty and one/many counts are independent; load error shows the approved retained-archive copy plus refresh.
    - Names/tags/notes/labels/relative paths/hashes are escaped by HTML context and wrap; only file evidence may scroll horizontally; matrices have one archive-specific accessible name.
    - Archive creation always requests complete deep capture, states source/song/slot/grid and `Device data will not change.`, then opens/focuses the new shelf summary on success.
    - Reduced motion disables reveal/shimmer/smooth effects while native focus, live status, text badges, and 44px targets remain usable.
  </behavior>
  <action>
Add the `archive shelf` tab and full-width `#view-archives` inside the existing `setTab()`/`render()` loop per D-09 through D-12, D-17, and D-18. Upgrade the three tabs in place to the UI-SPEC tablist/roving-focus contract; do not add a router. Replace `renderLibrary()` cards with independently pluralized verified/diagnostic counts and one shelf link. Implement exactly one `renderArchives()` using native `<details><summary>` rows, the existing `matrixSvg()` over archived parser data, `<time>`, `<code>`, contextual `esc()`/`attr()`, visible text badges, and the four expanded evidence groups. Render diagnostics in a separate landmark with neutral `needs attention` copy and no action markup. Implement the approved empty, loading, error, populated, partial, overflow, zero-one-many, and long-text states, responsive layout, 44px targets, focus transitions, polite announcements, and reduced-motion override. Update `backup()` to always request deep capture and to focus the newly created archive summary after load. Add tests before implementation that execute tab/focus/archive-success behavior where practical and statically prove one renderer, semantic disclosures, exact state copy, contextual escaping, matrix naming, responsive/reduced-motion contracts, and diagnostic action absence.
  </action>
  <acceptance_criteria>
    - [ ] All eight `02-UI-SPEC.md` UI Considerations are implemented and independently covered: empty, loading, error, populated, partial, overflow, zero-one-many, and long-text.
    - [ ] Archive Shelf is a top-level accessible tab and contains the only archive-row renderer; Songs contains counts and one link only.
    - [ ] Verified compact/expanded evidence matches D-10/D-11, remains readable at narrow widths, and derives its matrix from archived parser data.
    - [ ] Every manifest-derived value is escaped for its output context; hostile metadata renders as text, and diagnostics expose no restore/manual-free/target-slot controls.
    - [ ] Project verification is never labelled whole-song completeness or manual-free eligibility; separate text badges and inline explanations carry those facts.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern='archive shelf|shelf states|tab semantics|archive escaping|diagnostic actions|archive matrix' test/transaction.test.js</automated>
  </verify>
  <done>Dougal can open one first-class Archive Shelf, browse newest-first evidence disclosures in every approved state, and distinguish verified/project-only/complete records from action-free diagnostics without unsafe HTML or duplicated rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library classifier → `/api/state` | Stored manifest/file content becomes a sanitized browser-facing projection. |
| `/api/state` → browser DOM | Untrusted Unicode metadata, evidence strings, and diagnostic facts cross into HTML and attribute contexts. |
| Browser archive action → localhost POST boundary | Complete archive creation remains the only enabled mutation from this view. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | archive classification | high | mitigate | Consume Plan 01's strict current-byte classifier only; tests prove tampered/unknown/incomplete records route to diagnostics and lose every action. |
| T-02-02 | Tampering / Information Disclosure | snippet/sample-pack evidence paths | high | mitigate | Render only classifier-approved archive-relative evidence; preserve Plan 01 traversal/symlink tests and never resolve client-provided filesystem paths in the browser. |
| T-02-03 | Information Disclosure | shelf/diagnostic projection | high | mitigate | Explicit safe fields plus tests exclude absolute roots, settings credentials, stacks, and raw errors from JSON and DOM. |
| T-02-04 | Elevation of Privilege / Information Disclosure | `renderArchives()` | high | mitigate | Apply `esc()` or `attr()` at every DOM interpolation boundary, use native text semantics, and execute hostile Unicode/markup regression cases. |
| T-02-05 | Spoofing | source provenance | medium | mitigate | Display classifier-derived source kind/label/slot and keep verified, complete, and current eligibility as separate visible facts. |
| T-02-06 | Spoofing / Tampering | `opzdisk/` fallback | high | mitigate | Shelf may display fixture provenance but exposes no manual-free action in this plan; Plan 03 uses a device-only resolver and the full suite preserves pinned-source fallback tests. |
| T-02-07 | Tampering | automatic clear/device write | high | mitigate | UI contains no automatic-clear control/call and POST `/api/clear-slot` remains a tested central 409 fence; archive remains the sole mutation. |
</threat_model>

<verification>
- `node --test test/transaction.test.js`
- `node --check server.js`
- Extract the inline `<script>` from `app/index.html` and compile it with `new Function()` in an existing/static test or an equivalent one-line Node check.
- Confirm every one of the eight UI Considerations has a named automated assertion and diagnostics have explicit negative action assertions.
</verification>

<success_criteria>
- ARCH-05 is satisfied through one first-class Archive Shelf with deterministic order and complete compact/expanded evidence.
- Verified and diagnostic empty/error/loading/count states are independent and accessible.
- Long Unicode and hostile markup remain readable escaped text; no path/secret/raw error reaches the page.
- Diagnostics and project-only incomplete archives never acquire restore or manual-free actions, and no automatic device-clear write is exposed.
</success_criteria>

<output>
Create `.planning/phases/02-verified-archive-shelf-manual-freeing/02-02-SUMMARY.md` when done.
</output>
