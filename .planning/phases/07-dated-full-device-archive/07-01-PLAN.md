---
phase: 07-dated-full-device-archive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server.js
  - app/index.html
  - test/transaction.test.js
autonomous: true
requirements: [BULK-01, BULK-02, BULK-03, BULK-04]
---

# Phase 7 Plan: Dated Full-Device Archive

## Objective

Add one read-only bulk action that captures every occupied project and one shared sample-pack grid into a verified, dated device snapshot.

## Tasks

1. Add a source-pinned snapshot writer and strict scanner using the existing byte, parser, manifest, and mutation helpers.
2. Add one API route and one Songs-view action with dated result and per-slot evidence.
3. Add focused transaction and UI contract tests, then run the complete suite.

## Must Haves

- No device bytes change.
- One grid copy per snapshot, not per slot.
- Any member or source verification failure prevents publication.
- Published snapshots remain independently re-verifiable.

## Verification

- `node --check server.js`
- `node --test test/transaction.test.js`
- `git diff --check`
