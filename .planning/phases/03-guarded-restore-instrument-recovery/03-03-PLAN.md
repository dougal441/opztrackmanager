---
phase: 03-guarded-restore-instrument-recovery
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - server.js
  - app/index.html
  - test/transaction.test.js
autonomous: true
requirements:
  - REST-03
  - REST-04
  - REST-05
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-04/D-13: Whole-grid restoration is a separate explicit Archive Shelf action, independent of project target selection, and appears only for a freshly verified complete grid manifest."
    - "D-14/D-15: A complete verified current-grid recovery publishes before any live-grid mutation; the archived grid is staged and verified, replaces rather than overlays the live grid, and exact final evidence proves required files and absences."
    - "D-09 through D-12: Post-grid-mutation failure is non-success with the retained recovery reference and only claims rollback after exact restored-manifest verification; source loss forbids further writes."
    - "D-18 through D-20: Mounted and fixture operations use the same pinned transaction, while mounted acceptance performs same-byte project and whole-grid restores through direct API/filesystem access and proves final content plus unrelated-file preservation."
    - "D-21: Hardware evidence proves host Content Mode readback only; it never claims post-eject firmware loading, rejection status, or playback acceptance."
    - "Automatic clearing and authenticated op1.fun installation remain centrally fenced after all Phase 3 work."
  artifacts:
    - path: "server.js"
      provides: "Fresh grid eligibility, separate `/api/instruments/restore-grid`, exact staged grid replacement, verified rollback, and final route fences"
    - path: "app/index.html"
      provides: "Separate whole-grid Archive Shelf action, exact confirmation, shared status/result recovery UI, and mounted hardware-boundary copy"
    - path: "test/transaction.test.js"
      provides: "Exact replacement/absence/failure tests plus direct mounted same-byte project and whole-grid UAT"
  key_links:
    - from: "app/index.html restore whole instrument grid"
      to: "server.js POST /api/instruments/restore-grid"
      via: "fresh verified bundle ID and independent confirmation through the shared mutation wrapper"
      pattern: "restore whole instrument grid|/api/instruments/restore-grid"
    - from: "server.js grid restore"
      to: "archiveCapturedProject() deep recovery and archive samplepacks manifest"
      via: "verified current-grid preimage, staged incoming copy, root revalidation, directory replacement, and exact manifest readback"
      pattern: "manifestMatches|samplepacks|recovery"
    - from: "test/transaction.test.js mounted UAT"
      to: "real detected OP-Z plus localhost APIs"
      via: "before/after recursive evidence, same-byte writes, recovery IDs, final restoration in `finally`, and no screen control"
      pattern: "OPZ_HARDWARE_UAT|mounted restore|mounted grid"
---

<objective>
Complete REST-05 with a separately confirmed exact whole-grid restore, then prove project and grid write transactions against the mounted OP-Z without claiming firmware behavior the host cannot observe.

Purpose: Recover an archived instrument setup without stale live files, unprotected overwrites, fallback substitution, or fixture-only confidence.
Output: Exact staged whole-grid restore and rollback, distinct Archive Shelf UI, complete local failures tests, mounted same-byte UAT, and final route fences.
</objective>

## Phase Goal

**As a** person who owns an OP-Z, **I want to** restore a verified archive to a chosen slot while preserving the slot's current project and instrument grid, **so that** I can recover songs without silently losing existing device data.

<execution_context>
@/Users/dougalhanson/.codex/gsd-core/workflows/execute-plan.md
@/Users/dougalhanson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guarded-restore-instrument-recovery/03-CONTEXT.md
@.planning/phases/03-guarded-restore-instrument-recovery/03-RESEARCH.md
@.planning/phases/03-guarded-restore-instrument-recovery/03-PATTERNS.md
@.planning/phases/03-guarded-restore-instrument-recovery/03-UI-SPEC.md
@.planning/phases/03-guarded-restore-instrument-recovery/03-VALIDATION.md
@.planning/phases/03-guarded-restore-instrument-recovery/03-02-SUMMARY.md
@server.js
@app/index.html
@test/transaction.test.js

<interfaces>
Consume Plans 01–02 pinned-root, recovery publication/receipt, rollback, route-validation, `runMutation()`, and focused result contracts. Reuse `classifyArchive()`, `findBundle()`, `archiveCapturedProject()` with `deep: true`, `copyDir()`, `manifestMatches()`, `resolveChild()`, `snapshotRegularFiles()`, the existing hardware opt-in/server lifecycle, and direct `fs`/`crypto` checks. The archive's verified `samplepacks.files` manifest is the sole desired grid state; no inferred pack dependency or partial restore is introduced.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore one complete archived grid as a separate exact batch</name>
  <reversibility rating="costly">The operation replaces the complete live sample-pack tree, but a verified complete preimage is retained and rollback is evidence-checked before any rollback claim.</reversibility>
  <files>server.js, app/index.html, test/transaction.test.js</files>
  <read_first>
    - `server.js`: Plans 01–02 root/recovery/receipt helpers, `classifyArchive()`, `findBundle()`, `copyDir()`, `manifestMatches()`, grid recovery path, mutation inventory, and public error serializer.
    - `app/index.html`: Plan 01 Archive Shelf restore group/result UI, `runMutation()`, verified/complete grid facts, busy disabling, confirmations, and focus/alert handling.
    - `test/transaction.test.js`: schema deep-grid helpers, Plan 02 grid-recovery tests, failpoints, source substitution, public sanitation, and static/VM UI seams.
    - `03-CONTEXT.md`: Whole-Grid Instrument Recovery decisions D-13 through D-16 and Source Modes decisions D-18 through D-21.
    - `03-UI-SPEC.md`: whole-grid confirmation, independent availability, recovery states, exact copy, accessibility, and hardware-boundary text.
    - `03-VALIDATION.md`: 03-W0-05 plus stale-file, source-loss, rollback, and exact-absence failpoints.
  </read_first>
  <behavior>
    - `restore whole instrument grid` is rendered as a separated secondary action only when current archive classification proves `samplepacks.captured` and its complete manifest; it needs no project target and is never nested in project restore.
    - The browser uses the exact whole-grid confirmation and shared `runMutation()`/busy/result flow; it states that projects do not change and shows the retained recovery reference.
    - The server validates `{ file, auto }`, enters one guard, resolves/pins one source root and current live-grid manifest, freshly revalidates the archive directory/project/grid evidence, and pins the verified incoming samplepacks directory/manifest.
    - Before live-grid mutation, a complete deep automatic recovery archive of the current grid publishes and reclassifies successfully.
    - Incoming archived grid bytes are copied to a unique hidden sibling beneath the pinned root, every file rereads against the archive manifest, and root/current-grid evidence is rechecked before directory replacement.
    - The live grid is retained as a sibling, the staged tree becomes canonical, and exact final `manifestMatches()` proves every expected file and every required absence; no stale overlay file survives.
    - Any failure after live replacement begins is non-success with recovery ID; rollback renames/restores and exact-verifies the original grid only while root identity remains valid, otherwise `recovery_required`.
  </behavior>
  <action>
Write the whole-grid tracer and failure tests first, then add `/api/instruments/restore-grid` per D-04 and D-13 through D-16. Fresh classification must prove the stored project and complete `samplepacks` manifest before making the grid action eligible; return no path or Buffer through `/api/state`. Validate the request, acquire the guard before resolver work, capture one deterministic readable project solely to reuse `archiveCapturedProject(..., deep: true)` as the supported complete recovery artifact, and separately pin the source root/current grid manifest. Publish and reclassify that deep automatic backup before any live-grid rename.

Copy the archived grid into an exclusive hidden sibling under the captured root, verify it against the pinned archive manifest, then revalidate root identity, original live manifest, and archive evidence. Rename the live grid to a retained sibling and the staged grid to canonical; exact-verify final manifest including absences before removing only the retained live sibling. On error, never report success; restore/verify the retained tree only while the pinned root remains valid and keep the independent automatic backup in all outcomes. Clean abandoned stage names without crossing roots. Do not overlay files, loop through independent transactions, or call dynamic source/path helpers.

Add the separate UI-SPEC action beneath a divider in each eligible verified row, independent of slot selection. Use the exact confirmation/busy/success/failure/hardware-boundary copy, shared global disabling, focused result/alert, and retained recovery ID. Diagnostics, project-only archives, and stale complete archives expose no enabled grid action. Test exact stale-file removal, empty-manifest replacement, incoming/live manifest drift, backup/staging/first-rename/second-rename/readback failures, rollback verified versus forbidden, source loss with fixture fallback present, temp cleanup, zero project-byte change, response sanitation, independent button state, and hostile metadata escaping.
  </action>
  <verify>
    <automated>node --test --test-name-pattern='whole.grid restore|restore grid|exact grid|stale grid|grid rollback' test/transaction.test.js</automated>
  </verify>
  <done>A freshly verified complete archive can replace the whole grid through its own guarded action, with a verified complete preimage and exact manifest proof for success or rollback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass direct mounted project and whole-grid same-byte acceptance</name>
  <precondition>A real OP-Z is mounted in Content Mode with a detected `device: true` source, at least one readable parsable project, and a readable sample-pack grid; fixture fallback evidence cannot satisfy this gate.</precondition>
  <files>test/transaction.test.js</files>
  <read_first>
    - `test/transaction.test.js`: existing mounted archive/manual-free UATs, `snapshotRegularFiles()`, HTTP helpers, temporary library/meta hooks, hardware skip flag, and Plans 01–03 restore tests.
    - `server.js`: `/api/backup`, `/api/restore`, `/api/instruments/restore-grid`, mounted source discovery, archive classifier, test hooks, and recovery response shape.
    - `03-VALIDATION.md`: Mounted UAT Contract and 03-W0-08.
    - `03-CONTEXT.md`: direct API/filesystem-only UAT, same-byte writes, no screen control, and Content Mode evidence boundary D-18 through D-21.
  </read_first>
  <behavior>
    - Hardware tests remain skipped unless `OPZ_HARDWARE_UAT=1`, but the phase gate invokes them with that flag and fails if no real device source is detected.
    - Before mutation, the test snapshots every regular file under the mounted root by relative path, SHA-256, byte length, mode, and modification time and retains exact initial bytes for every canonical project/grid file it may touch.
    - Through the real ephemeral localhost server, the test creates a verified complete archive, submits an explicit exact target fingerprint, restores that slot's own exact bytes, and requires success plus a verified automatic recovery reference and exact parser/readback evidence.
    - Through the separate real grid endpoint, it restores the just-captured whole grid over itself and requires success, a distinct complete recovery reference, and exact final manifest equality.
    - No unrelated regular-file path/content/mode/mtime changes. Canonical target/grid files may have write-related mtime changes during exercise, but their content hashes and modes match the initial snapshot; initial bytes are restored and fully reverified in `finally` if any canonical content differs.
    - The test performs no screen control, browser automation, clear, eject, disconnect, hardware keypress, normal-mode load, playback, or rejection-folder claim.
  </behavior>
  <action>
Add two named opt-in mounted tests or one combined test whose name matches both hardware patterns. Detect the real device source through application state/discovery rather than substituting `opzdisk/`; direct archive output and metadata to disposable laptop paths. Snapshot the whole mounted regular-file tree and retain initial canonical bytes. Use the real API to create a complete verified archive from a readable mounted slot, then restore the archive's exact project bytes to that explicitly fingerprinted same slot and call the independent whole-grid restore using the same archive. Require HTTP success only with recovery IDs, mounted source facts, exact target reread/parse, and exact grid manifest evidence.

In `finally`, if any canonical byte differs, restore only from the retained initial Buffers while the same mount identity is still present, then reread and require exact SHA-256 equality. Compare the final tree: all relative paths, hashes, lengths, and modes equal the initial snapshot; all unrelated file mtimes equal as well. Emit a deterministic evidence digest in test diagnostics. Run no screen/UI control and make no post-eject firmware statement. Finish with the full local suite, syntax/inline-script checks, and route inventory assertions proving `/api/clear-slot` and `/api/op1fun/download` remain fenced.
  </action>
  <verify>
    <automated>OPZ_HARDWARE_UAT=1 node --test --test-name-pattern='mounted restore|mounted grid' test/transaction.test.js</automated>
  </verify>
  <done>Real mounted project and whole-grid writes complete only after verified recovery and exact Content Mode readback, final device content is proven preserved, and no screen or firmware acceptance claim is used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verified archive grid → staged mounted tree | Laptop archive files and declared absences define the complete replacement state. |
| Current mounted grid → automatic recovery archive | All live pack bytes must be captured and verified before directory replacement. |
| Staged/retained siblings → canonical samplepacks | Multi-rename removable-volume behavior can fail between operations. |
| Automated UAT → real mounted OP-Z | A test exercises real writes and must preserve initial content without display or physical actions. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Tampering | archived whole-grid input | high | mitigate | Fresh strict archive classification, exact manifest/regular-file evidence, contained pinned directory, staged copy, and pre-write revalidation; stale/tampered archive tests block. |
| T-03-18 | Tampering | current grid preimage | high | mitigate | Capture exact live manifest and publish/reclassify a complete deep automatic recovery before any live rename; backup failure proves zero mutation. |
| T-03-19 | Tampering / Denial of Service | batch directory replacement | high | mitigate | Hidden same-root stage, retained live sibling, root/current/incoming rechecks, exact final manifest including absences, and failpoints at every rename/readback boundary. |
| T-03-20 | Spoofing | source disappearance/fallback | high | mitigate | Use one captured root identity for staging, mutation, verification, and conditional rollback; source loss forbids writes and fallback-substitution tests assert local fixture untouched. |
| T-03-21 | Repudiation | grid restore result | high | mitigate | Success requires exact final evidence; all later failures attach the retained recovery ID and truthful verified-rollback or recovery-required state in focused text UI. |
| T-03-22 | Information Disclosure | archive/grid errors | high | mitigate | Public response and UI expose only bounded archive/source/receipt facts and fixed guidance, never mounted/library/temp paths, file lists beyond approved shelf evidence, internals, or raw errors. |
| T-03-23 | Tampering | mounted UAT | high | mitigate | Same-byte operations, whole-root pre/post evidence, retained initial Buffers, `finally` restoration, exact SHA-256/mode/path checks, disposable laptop output, and hard device-source precondition. |
| T-03-24 | Elevation of Privilege | deferred destructive routes | high | mitigate | Central inventory and browser assertions keep `/api/clear-slot` and `/api/op1fun/download` unavailable after all local Phase 3 routes are enabled. |
| T-03-SC | Tampering | package supply chain | low | accept | This plan installs nothing and uses only existing Node/browser/filesystem/parser code. |
</threat_model>

<verification>
- `node --test --test-name-pattern='whole.grid restore|restore grid|grid rollback' test/transaction.test.js`
- `node --test test/transaction.test.js`
- `node --check server.js`
- Compile and VM-exercise inline restore functions without opening or controlling a browser.
- `OPZ_HARDWARE_UAT=1 node --test --test-name-pattern='mounted restore|mounted grid' test/transaction.test.js`
- Review mounted diagnostics for a deterministic initial/final content-evidence digest and no fallback, screen, eject, clear, firmware-load, rejection, or playback action.
</verification>

<success_criteria>
- REST-05: Whole-grid restore is independent from project restore and starts only after a complete verified grid recovery exists.
- REST-03/REST-04: Incoming and written grid evidence is fresh/exact, and every uncertain post-mutation outcome is non-success with a retained recovery reference.
- Local failpoints prove exact replacement, required absences, rollback truth, source pinning, sanitation, and project-byte preservation.
- Mounted API/filesystem UAT proves same-byte project and grid writes, recovery receipts, exact Content Mode readback, and final content preservation without screen control.
- Automatic clear and authenticated op1.fun install remain unavailable.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Explicit verified archive restore preserves current project and instrument grid | 03-01, 03-03 | COVERED | Project and whole-grid operations are independently guarded. |
| REQ | REST-01 | Explicit target plus current-song preview | 03-01 | COVERED | No initial target; exact live fingerprint. |
| REQ | REST-02 | Verified automatic target backup before overwrite | 03-01, 03-02 | COVERED | Project restore and both swap targets publish first. |
| REQ | REST-03 | Fresh archive validation and output reread/byte check | 03-01, 03-03 | COVERED | Project and grid current evidence drive writes and success. |
| REQ | REST-04 | Recovery reference and non-success after mutation failure | 03-01, 03-02, 03-03 | COVERED | One sanitized retained/rolled-back/recovery-required contract. |
| REQ | REST-05 | Project default; separate protected whole-grid restore | 03-03 | COVERED | Independent action and deep verified recovery. |
| RESEARCH | — | Split root identity from original-project validation | 03-01 | COVERED | Shared root-cause source assertion. |
| RESEARCH | — | Pin classifier-approved archive bytes | 03-01 | COVERED | Restore consumes the freshly evidenced Buffer. |
| RESEARCH | — | Protect both sides of project swap | 03-02 | COVERED | Two backups before either write. |
| RESEARCH | — | Exact grid replacement, not overlay | 03-03 | COVERED | Manifest defines files and absences. |
| RESEARCH | — | Local instrument actions share complete recovery | 03-02 | COVERED | Move/remove/import/snapshot only; remote install fenced. |
| RESEARCH | — | Native Archive Shelf UI and shared mutation state | 03-01, 03-02, 03-03 | COVERED | No page, modal, component, or dependency added. |
| RESEARCH | — | Direct mounted same-byte acceptance | 03-03 | COVERED | API/filesystem-only hard gate. |
| CONTEXT | D-01–D-04 | Explicit target intent and project-only default | 03-01, 03-03 | COVERED | Selection, stale binding, fresh classification, separate grid. |
| CONTEXT | D-05–D-08 | Guarded project transaction | 03-01 | COVERED | Existing guard/source/publisher and verified output ordering. |
| CONTEXT | D-09–D-12 | Failure and recovery receipt | 03-01, 03-02, 03-03 | COVERED | Every mutation path shares truthfully verified recovery state. |
| CONTEXT | D-13–D-17 | Whole-grid and existing local recovery actions | 03-02, 03-03 | COVERED | Grid batch, local pack actions, protected swap. |
| CONTEXT | D-18–D-21 | Source modes and mounted UAT boundary | 03-01, 03-03 | COVERED | Pinned fixture/device contract and host-readback-only claim. |

Deferred automatic clearing, partial/per-song grid restore, firmware playback acceptance, and authenticated op1.fun installation are intentionally excluded exactly as recorded in `03-CONTEXT.md`.

<output>
Create `.planning/phases/03-guarded-restore-instrument-recovery/03-03-SUMMARY.md` when done.
</output>
