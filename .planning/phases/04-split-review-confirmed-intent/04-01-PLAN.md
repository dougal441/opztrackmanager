---
phase: 04-split-review-confirmed-intent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server.js
  - app/index.html
  - test/transaction.test.js
autonomous: true
requirements: [SPLT-01, SPLT-02, SPLT-03]
estimate:
  tokens: 14000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A slot with plausible disjoint-song evidence is visibly suggested for review, while a non-split or ambiguous slot is not silently divided."
    - "The review surface explains chain, pattern-cluster, and track-profile evidence and lets the user edit both exact memberships and names before confirming or rejecting."
    - "Confirmation persists parent-content-hash provenance and exact memberships/names; rejection and review edits do not alter the source project bytes."
  artifacts:
    - "server.js exposes deterministic split evidence and a validated confirmation persistence path."
    - "app/index.html renders an accessible split-review panel from state and submits only explicit confirmation."
    - "test/transaction.test.js covers detection, review payload validation, persistence, rejection, and source-byte immutability."
  key_links:
    - "parseProject/parseTrackChunks output → deterministic evidence classifier → /api/state split-review projection"
    - "reviewed parent hash and exact memberships → validated confirmation route → atomic data/meta.json persistence"
    - "confirmation/rejection UI actions → no project filesystem write"
---

<objective>
Deliver an evidence-only split-review workflow for likely two-song OP-Z projects, with explicit user-controlled membership and naming confirmation that records immutable parent provenance.

Purpose: Satisfy SPLT-01 through SPLT-03 without allowing heuristic detection to reinterpret or mutate a real project.
Output: Server evidence/provenance handling, browser review UI, and local integration coverage.
</objective>

<execution_context>
@/Users/dougalhanson/.codex/gsd-core/workflows/execute-plan.md
@/Users/dougalhanson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-split-review-confirmed-intent/04-CONTEXT.md
@parser.js
@server.js
@app/index.html
@test/transaction.test.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one reviewed split from slot evidence to confirmed intent</name>
  <files>server.js, app/index.html, test/transaction.test.js</files>
  <behavior>
    - Test 1: A project with disjoint saved chains and separated occupied pattern groups produces a deterministic review suggestion containing both memberships and explainable chain/cluster/track-profile evidence.
    - Test 2: A confirmation with the reviewed parent SHA-256, two non-empty disjoint membership lists covering the selected patterns, and bounded names persists under the parent hash and returns the exact saved intent.
    - Test 3: The confirmation path leaves the original project bytes and hash unchanged.
  </behavior>
  <action>Implement one end-to-end path per D-01 and D-02: derive deterministic split evidence from existing parsed chains, musical occupied-pattern clusters, and per-pattern musical track profiles; add the bounded `/api/state` projection and a validated explicit confirmation route using the existing atomic metadata writer. In the inline UI, render an accessible review panel for suggested slots showing evidence, editable exact pattern membership for each half, names, and confirm/reject controls per D-03. Bind confirmation to the current parent project hash and persist `meta.splits[parentHash]` with stable parent hash, exact half pattern arrays, names, and confirmation timestamp per D-04. Reject malformed/stale/overlapping memberships and never call any project write, archive, synthesis, or device mutation helper. Add the end-to-end integration test before implementation and keep all values bounded and sanitized at the request boundary.</action>
  <verify>
    <automated>node --test test/transaction.test.js</automated>
  </verify>
  <done>The running local app can show one explainable suggestion, accept an explicitly edited two-half review, persist exact parent-hash provenance, reject invalid/stale intent, and leave source bytes byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify split edge cases and immutable rejection behavior</name>
  <files>test/transaction.test.js</files>
  <behavior>
    - Test 1: Connected/overlapping chains, one-cluster projects, and empty/non-musical patterns do not create an unsafe split suggestion.
    - Test 2: Edited memberships and names remain browser/request-local until confirmation; rejection leaves no persisted split record.
    - Test 3: Replaying confirmation against changed parent bytes fails closed and does not write project data.
  </behavior>
  <action>Extend the existing integration harness with focused regression cases for D-01 through D-04, using read-only fixture copies and byte/hash snapshots. Assert deterministic evidence ordering, exact membership validation, stale-parent rejection, sanitized names, rejection non-persistence, and zero writes beneath the source project path. Do not add a test framework or device/browser-control dependency.</action>
  <verify>
    <automated>node --test test/transaction.test.js && node --check server.js</automated>
  </verify>
  <done>Split detection and confirmation remain fail-closed across ambiguous, stale, malformed, rejected, and source-immutability cases, with the complete local suite passing.</done>
</task>

</tasks>

## Source audit

| Source | ID | Feature / constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Evidence-only split review with confirmed intent and unchanged original | 04-01 | COVERED |
| REQ | SPLT-01 | Surface likely split evidence from chains, clusters, and track profiles | 04-01 | COVERED |
| REQ | SPLT-02 | Inspect/edit memberships and names; confirm or reject | 04-01 | COVERED |
| REQ | SPLT-03 | Persist stable provenance and exact membership without source mutation | 04-01 | COVERED |
| CONTEXT | D-01 | Detection remains evidence-only; no automatic split decision | 04-01 | COVERED |
| CONTEXT | D-02 | Explain all three evidence classes with chain signal strongest | 04-01 | COVERED |
| CONTEXT | D-03 | Explicit editable confirmation or rejection | 04-01 | COVERED |
| CONTEXT | D-04 | Parent-hash provenance, exact membership, immutable original | 04-01 | COVERED |
| RESEARCH | — | No phase RESEARCH.md; existing parser/server patterns are sufficient | 04-01 | COVERED |

<verification>
Run `node --test test/transaction.test.js` and `node --check server.js`; inspect the test fixture's before/after project hash and confirm no route writes under `opzdisk/projects/`.
</verification>

<success_criteria>
SPLT-01, SPLT-02, and SPLT-03 are covered by an explainable evidence projection, explicit editable review/confirm/reject UI, stable parent-hash exact-membership persistence, and passing local immutability regression tests. No synthesis, archive, restore, clearing, or device mutation is introduced.
</success_criteria>

<output>
Create `.planning/phases/04-split-review-confirmed-intent/04-01-SUMMARY.md` when done.
</output>
