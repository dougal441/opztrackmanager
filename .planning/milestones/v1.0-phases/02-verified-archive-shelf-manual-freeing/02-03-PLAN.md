---
phase: 02-verified-archive-shelf-manual-freeing
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - server.js
  - app/index.html
  - test/transaction.test.js
autonomous: true
requirements:
  - SAFE-04
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-13/D-14: `prepare manual freeing` exists only for a freshly revalidated complete archive whose known source slot, mounted-device identity, and current project hash exactly match; checklist reveal repeats the same read-only checks."
    - "D-15: The inline checklist gives the official safe-eject, project-selection, on-device clear, reconnect-in-content-mode, and refresh sequence with exact slot/song/source identity."
    - "D-16: Mount loss, archive change, or source mismatch stops the flow with reconnect/recovery guidance while retaining the archive."
    - "All acknowledgement and checklist controls are browser-local instructions; steps 1-4 and final confirmation perform zero device writes and never resolve `opzdisk/` as a substitute."
    - "Phase 2 never issues or exposes an automatic device-clear write; POST `/api/clear-slot` remains unavailable and no browser control calls it."
    - "Project verification alone is never presented as whole-song completeness or manual-free eligibility; diagnostics and incomplete archives never receive the checklist action."
    - "The undocumented post-clear representation remains fail-closed: changed/absent/zero-pattern states are not called empty or success without a later hardware-clearing acceptance fixture."
    - "An opt-in mounted UAT archives and preflights through direct API/filesystem access and proves every regular file beneath the mounted root retains the same relative path, size, mode, modification time, and SHA-256 without screen control or physical clearing."
  artifacts:
    - path: "server.js"
      provides: "One device-only read-only manual-free inspector and GET endpoint using the shared archive classifier"
    - path: "app/index.html"
      provides: "Eligibility-gated official checklist, identity acknowledgement, focus/live status, and fail-closed final refresh"
    - path: "test/transaction.test.js"
      provides: "Local fail-closed/read-only regressions plus direct mounted non-mutation UAT"
  key_links:
    - from: "app/index.html prepare manual freeing"
      to: "GET /api/manual-free"
      via: "a fresh read with a positively validated bundle ID immediately before checklist reveal and final refresh"
      pattern: "/api/manual-free|prepare manual freeing"
    - from: "server.js manual-free inspector"
      to: "Plan 01 archive classifier and mounted slot bytes"
      via: "current archive revalidation plus `findDeviceRoot()`-only resolution, request-local `captureSource()`/`assertCapturedSource()`, exact provenance/slot/hash/length comparison, and no write helper"
      pattern: "findDeviceRoot|manualFree|sha256"
    - from: "test/transaction.test.js mounted UAT"
      to: "mounted OP-Z tree and localhost API"
      via: "before/after recursive file evidence around archive and manual-free GET requests"
      pattern: "OPZ_HARDWARE_UAT|manual free mounted UAT"
---

<objective>
Satisfy SAFE-04 with a device-only, read-only preflight and an accessible official manual clearing checklist, then prove the app itself performs no mounted-device mutation.

Purpose: Let Dougal physically free the exact archived slot while automatic clearing remains fenced and every app-side check fails closed.
Output: One fresh manual-free inspection path, an exact-identity checklist flow, deterministic non-success outcomes, and a mounted non-mutation UAT.
</objective>

## Phase Goal

**As a** Dougal, **I want to** create and inspect a complete verified archive before following guided on-device manual clearing, **so that** I can free an OP-Z slot without losing the complete song.

<execution_context>
@/Users/dougalhanson/.codex/gsd-core/workflows/execute-plan.md
@/Users/dougalhanson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-CONTEXT.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-RESEARCH.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-PATTERNS.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-UI-SPEC.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-VALIDATION.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-02-SUMMARY.md
@server.js
@app/index.html
@test/transaction.test.js

<interfaces>
Consume the Plan 01 classifier and Plan 02 shelf records. Reuse `findDeviceRoot()` for mounted-device-only discovery, `captureSource(slot, source)`, `assertCapturedSource(captured)`, `projFile(slot, source)`, `resolveChild()`, positive bundle validation, `sha256(buf)`, `parseProject(buf)`, `api(path)` for GET requests, `renderArchives()`, existing live status/toast/focus rules, `request()`, `requestJson()`, `tempRoots()`, `testHooks`, and the existing `OPZ_HARDWARE_UAT` opt-in pattern. Do not call `getSource()` from manual-free inspection because it may select `opzdisk/`.
</interfaces>
</context>

<artifacts_this_plan_produces>
- `GET /api/manual-free?file={bundleId}` as a fresh, sanitized, read-only inspection path.
- Current shelf eligibility derived from the same inspector without persisted tokens or flags.
- An inline official OP-Z manual-free checklist gated by exact archive/source/slot identity.
- Local and mounted evidence that every app-side step preserves device bytes and the archive.
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate the official checklist behind one fresh device-only read path</name>
  <files>server.js, app/index.html, test/transaction.test.js</files>
  <read_first>
    - `server.js`: `findDeviceRoot()`, `getSource()`, `projFile()`, Plan 01 classifier, `findBundle()`, `/api/state`, route fences, request validation, and public error handling.
    - `app/index.html`: Plan 02 `renderArchives()`, `api()`, toast/live status, tab focus, native disclosure structure, and mutation-control wiring.
    - `test/transaction.test.js`: source fallback tests, route inventory/fence tests, read-only request patterns, source-tree preservation cases, and static UI action-absence tests.
    - `02-CONTEXT.md`: Guided Manual Freeing decisions D-13 through D-16.
    - `02-UI-SPEC.md`: manual-free eligibility, exact checklist/copy, focus/ARIA behavior, and final outcome copy.
    - `02-RESEARCH.md`: Pattern 4, Official Manual Sequence, Pitfalls 6 through 8, and Resolved Questions 1–3.
  </read_first>
  <behavior>
    - Shelf action appears only when current inspection says the archive is verified/complete, sanitized archive provenance identifies a mounted source and known slot, a mounted OP-Z is freshly captured for this request, and current slot SHA-256/length equals the archived project.
    - `GET /api/manual-free?file=...` positively validates/contains the bundle ID, reruns archive classification, resolves only a mounted OP-Z, reuses `captureSource()` for request-local canonical root/dev/inode plus exact slot bytes, and calls `assertCapturedSource()` before returning sanitized eligibility/relation/guidance facts without filesystem mutation.
    - Preflight failure or mount disappearance focuses the approved stop message and never reveals the checklist.
    - Successful preflight reveals the exact archive ID/song/slot/source and the five official steps; identity acknowledgement is required before steps 1-4 become usable.
    - Checkbox changes perform no request; final `refresh and verify slot` performs only the same GET and reports archived song still present, mount unavailable, or changed/unclassified stop. It does not claim confirmed empty from missing files, no used patterns, or unknown bytes.
    - POST `/api/clear-slot` remains a 409 later-phase fence, and no shelf/checkbox/final-refresh code calls a mutation route or write helper.
  </behavior>
  <action>
Add one focused manual-free inspector in `server.js` and expose it through a GET route, per D-13 through D-16 and Research Resolved Question 1. Validate/contain the bundle ID, consume the shared archive classifier, and require verified/complete evidence whose sanitized provenance says mounted source and known slot. Call `findDeviceRoot()` directly, form the mounted source object, and reuse `captureSource()` to pin the current request's canonical root/dev/inode and exact slot Buffer; never call the fallback-capable resolver. Compare archive provenance plus current project length/SHA-256, then call `assertCapturedSource()` immediately before returning so disappearance, root replacement, or slot-byte change invalidates the result. Persist no filesystem identity or opaque fingerprint and make no remount-stability assumption: every request captures fresh current-process identity. Return only public facts and a stable relation. Derive current shelf eligibility through this same inspector without caching or persisting an eligibility flag. Keep every filesystem write/delete/rename function and `withMutation()` outside this call graph and leave POST `/api/clear-slot` fenced.

Extend the existing `renderArchives()` evidence/action group with the UI-SPEC `prepare manual freeing` flow. Call the GET endpoint immediately before revealing instructions, show `aria-busy`/live text, focus stop or checklist headings appropriately, require the exact-slot identity checkbox, and render the official safe-eject/select/clear/reconnect/refresh text including slot 10→value key 0 mapping. Steps 1-4 are labelled local checkboxes only. The final button repeats the GET and fails closed for any unproven changed or absent representation; keep the archive visible in every outcome. Add tests first that instrument the real server/helpers and browser flow to prove exact-match gating, archive/source/slot changes, mount loss, hostile bundle IDs, diagnostics/incomplete action absence, zero write-helper calls, no fallback resolver call, unchanged archive/source snapshots, exact official copy, checkbox-local behavior, and continued clear-route fencing.
  </action>
  <acceptance_criteria>
    - [ ] SAFE-04 unclassified edge is resolved: checklist reveal requires a freshly captured current-process mounted identity plus exact archive provenance/slot/hash/length match, final source revalidation, zero device writes, and no `opzdisk/` fallback.
    - [ ] The browser never exposes or issues an automatic device-clear write; POST `/api/clear-slot` remains unavailable and no manual-flow control calls it.
    - [ ] Project-only, incomplete, diagnostic, source-mismatched, and mount-unavailable records have visible reasons and no usable checklist action.
    - [ ] The five-step guidance matches the official project/disk-mode sequence, identifies the exact song/slot/source, maps slot 10 to key 0, and retains the archive for every outcome.
    - [ ] No file absence, no-pattern parse, or changed hash is reported as a successfully empty slot; the undocumented representation remains a stop condition for the later hardware-clearing phase.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern='manual free|device-only|request-local source|source substitution|no device write|source fallback|clear route|manual checklist' test/transaction.test.js</automated>
  </verify>
  <done>A freshly matched complete archive can reveal the exact official checklist, while every mismatch fails closed, no diagnostic/incomplete archive gains an action, and no app-side path can clear or write the OP-Z.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove archive and preflight preserve every regular file beneath the mounted root</name>
  <precondition>A real OP-Z is mounted in content mode at the device root used by the existing hardware UAT, and at least one non-empty slot is readable.</precondition>
  <files>test/transaction.test.js</files>
  <read_first>
    - `test/transaction.test.js`: existing `mounted API archive UAT`, server lifecycle, temporary library/meta hooks, hashing helpers, and hardware skip gate.
    - `server.js`: exported server/test hooks, mounted source detection, `/api/backup`, `/api/state`, and `/api/manual-free` from Task 1.
    - `02-VALIDATION.md`: `02-W0-04` and `02-W0-05` mounted non-mutation criteria.
    - `02-CONTEXT.md`: direct mounted API/filesystem UAT and no-screen-control constraint.
  </read_first>
  <behavior>
    - The opt-in test selects a readable mounted slot and records a deterministic recursive snapshot of every regular file beneath the mounted root before any request: relative path, byte length, permission mode, modification time, and SHA-256.
    - It directs archive output and metadata to temporary laptop paths, invokes the real localhost archive API for a complete archive, confirms the resulting bundle revalidates, and invokes the real manual-free GET preflight.
    - Preflight is eligible only for the exact mounted slot/archive identity and becomes ineligible under a test-injected mismatch without selecting `opzdisk/`.
    - After all API calls, the complete mounted-root regular-file snapshot equals the before snapshot exactly; the verified laptop archive still exists.
    - The UAT performs no physical clear, no screen control, no filesystem write under the mount, and no empty-slot success assertion.
  </behavior>
  <action>
Extend the existing `OPZ_HARDWARE_UAT=1` pattern with one named `manual free mounted UAT`. Use direct Node filesystem reads and the exported localhost server only. Walk the entire mounted root recursively without following symlinks and snapshot every regular file as a sorted record of archive-independent relative path, byte length, permission mode, modification time, and SHA-256 before the test. Point library/meta outputs to a disposable laptop directory, create a complete archive through `/api/backup`, re-read its stored evidence, then call `/api/manual-free` and assert exact-match eligibility, request-local source capture/final revalidation, and mounted provenance. Repeat the same complete regular-file walk afterward and require exact snapshot equality; require the laptop archive still exists. Do not interact with the display, press hardware controls, delete/rename/write mounted files, or encode any cleared-slot heuristic. Keep the test skipped unless the existing hardware flag is explicitly set.
  </action>
  <acceptance_criteria>
    - [ ] The direct mounted UAT archives, reads, and preflights a real slot while relative path, size, mode, modification time, and SHA-256 for every regular file beneath the mounted root remain exactly unchanged.
    - [ ] The UAT proves the laptop archive is verified/complete and retained after preflight.
    - [ ] The UAT contains no screen control, physical clearing, mounted write, fallback substitution, or successful empty-slot assertion.
    - [ ] The normal test suite remains deterministic with this hardware test skipped by default.
  </acceptance_criteria>
  <verify>
    <automated>OPZ_HARDWARE_UAT=1 node --test --test-name-pattern='manual free mounted UAT' test/transaction.test.js</automated>
  </verify>
  <done>The real mounted OP-Z has automated before/after evidence proving Phase 2 archive and manual-free inspection are read-only, with the verified laptop archive retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser bundle selector → manual-free GET | A user-controlled identifier selects a laptop archive for safety-critical physical guidance. |
| Laptop archive → mounted OP-Z | Stored provenance/evidence is compared with live device identity and exact slot bytes. |
| Browser checklist → human/device | UI guidance may cause a physical destructive action even though the app performs no write. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | selected archive/manifest | high | mitigate | Positively validate and canonically contain the bundle ID, rerun the strict classifier immediately before guidance and refresh, and reject diagnostics/tampering. |
| T-02-02 | Tampering / Information Disclosure | archive/snippet evidence paths | high | mitigate | Manual inspection consumes classifier-approved archive-relative evidence only; traversal/symlink regression suite remains blocking. |
| T-02-03 | Elevation of Privilege / Information Disclosure | checklist rendering | high | mitigate | Escape archive/song/source facts by context, use fixed official instruction text and native controls, and test hostile metadata as inert text. |
| T-02-04 | Spoofing / Tampering | mounted source versus `opzdisk/` | high | mitigate | Call `findDeviceRoot()` directly, reuse request-local `captureSource()`/`assertCapturedSource()`, require mounted provenance plus exact slot/hash/length, persist no fingerprint, and assert the fallback resolver is never called. |
| T-02-05 | Information Disclosure | manual-free response/errors | high | mitigate | Return only sanitized bundle ID, song/slot/source label, stable relation, and guidance; tests exclude paths, credentials, settings, stacks, and raw errors. |
| T-02-06 | Tampering | accidental device write/automatic clear | high | mitigate | Keep the route GET-only and outside all write/mutation helpers, preserve the POST fence, instrument local tests for zero writes, and require exact before/after evidence for every regular file beneath the mounted root. |
| T-02-07 | Spoofing | unproven empty-slot result | high | mitigate | Treat absent/changed/no-pattern representations as unclassified stop outcomes and prohibit success wording until a later hardware-clearing acceptance supplies authoritative evidence. |
| T-02-08 | Repudiation | wrong physical slot guidance | medium | mitigate | Display archive ID/song/source/slot, require exact identity acknowledgement, map slot 10 explicitly to value key 0, and announce every stop/result in the live region. |
</threat_model>

<verification>
- `node --test test/transaction.test.js`
- `node --check server.js`
- Compile the inline browser script through the existing static syntax check.
- `OPZ_HARDWARE_UAT=1 node --test --test-name-pattern='manual free mounted UAT' test/transaction.test.js`
- Review the mounted UAT output for exact before/after equality of relative path, byte length, permission mode, modification time, and SHA-256 for every regular file beneath the mounted root; no screen-control or physical-clear step is part of acceptance.
</verification>

<success_criteria>
- SAFE-04 is satisfied with exact-match, device-only, read-only guidance and no automatic clearing surface.
- The app never presents project verification as whole-song completeness/manual-free eligibility and never gives diagnostics/incomplete archives a destructive guidance action.
- Local tests prove every checklist read is non-mutating and fallback-free; mounted UAT proves the real device tree remains unchanged.
- Any unrecognized post-clear state stays fail-closed and the verified archive remains retained.
</success_criteria>

<output>
Create `.planning/phases/02-verified-archive-shelf-manual-freeing/02-03-SUMMARY.md` when done.
</output>
