---
phase: 06-hardware-gated-automatic-clearing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server.js
  - app/index.html
  - test/transaction.test.js
  - data/clear-acceptance.json
autonomous: false
requirements: [CLEAR-01, CLEAR-02, CLEAR-03]
estimate:
  tokens: 12000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Automatic clearing cannot be enabled or invoked without a complete acceptance record for exactly the delete-project-file method (D-01, D-02).
    - A clear request verifies a complete archive and retains its verified recovery before deleting the exact captured slot file (D-03, D-04).
    - Success is reported only after the same mounted source reconnects and the expected empty-slot state is directly confirmed; uncertainty is non-success (D-04, D-05).
    - A failed or unconfirmed clear leaves the recovery archive retained and tells the user how to reconnect and restore it (D-03, D-05).
  artifacts:
    - server.js implements the acceptance record validation, derived enablement, pinned clear transaction, and post-reconnect confirmation boundary.
    - app/index.html exposes automatic clearing only when server state says it is enabled and renders recovery guidance on non-success.
    - test/transaction.test.js proves fixture clearing, archive-first ordering, source pinning, fail-closed behavior, and acceptance gating.
    - data/clear-acceptance.json is a bounded, non-secret acceptance record; it is absent or incomplete until direct hardware UAT passes.
  key_links:
    - Verified archive and automatic clear share the captured source token/fingerprint and mutation guard.
    - Acceptance method/outcomes are compared server-side with the clear method and current project hash, never trusted from UI state.
    - Post-reconnect state is read from the original mounted root and cannot fall back to opzdisk.
---

<objective>
Enable one hardware-proven automatic slot-clearing path while preserving the existing verified archive, source-pinning, and manual-free safety contracts.

Purpose: CLEAR-01..03 require automatic clearing to be unavailable by default and to remain recoverable whenever hardware behavior is ambiguous.
Output: Fixture-proven delete-project-file transaction, server-derived acceptance gate, guarded UI action, and a direct sacrificial-device UAT path that can record acceptance without screen control.
</objective>

<execution_context>
@/Users/dougalhanson/.codex/gsd-core/workflows/execute-plan.md
@/Users/dougalhanson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-hardware-gated-automatic-clearing/06-CONTEXT.md
@.planning/phases/02-verified-archive-shelf-manual-freeing/02-03-SUMMARY.md
@.planning/phases/03-guarded-restore-instrument-recovery/03-03-SUMMARY.md
@server.js
@app/index.html
@test/transaction.test.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace verified archive through gated delete and confirmation</name>
  <files>server.js, app/index.html, test/transaction.test.js, data/clear-acceptance.json</files>
  <behavior>
    - Without a complete exact-method acceptance record, state and clear requests expose automatic clearing as unavailable and the route performs no filesystem mutation (D-01, D-02).
    - With fixture acceptance, a clear request captures and verifies the complete archive first, deletes only the pinned `projectNN.opz`, revalidates the pinned root, and returns a recovery receipt; any injected failure before confirmation leaves the source/recovery safe and returns explicit recovery guidance (D-03, D-04, D-05).
    - Acceptance evidence is bounded, method-specific, source/device-specific, and cannot be made valid by UI-supplied post-reconnect claims (D-02, D-04).
  </behavior>
  <action>Replace the `/api/clear-slot` phase fence with the smallest server-owned implementation for D-01: validate the exact slot, source token, and current fingerprint; require a complete verified archive (including deep sample-pack evidence) to be published through the existing archive transaction; retain the verified automatic recovery; then delete only the captured slot project file. Add a bounded acceptance record reader/validator and derived `clearEnabled` state requiring the exact delete-project-file method, passing fixture evidence, and all recorded sacrificial-device outcomes including eject, reconnect, and confirmed empty-slot post-reconnect state. Keep the source pinned through deletion and final read; never call the fallback resolver after a captured device is lost. Return stable non-success errors with the recovery reference and reconnect/restore instructions whenever deletion or confirmation is not proven. Wire the existing archive shelf/manual-free UI to show one automatic-clear control only from server-derived eligibility and to render the returned recovery guidance; keep manual clearing available whenever the gate is false. Extend the existing dependency-free tests with fixture-first ordering, no-gate/no-mutation, source substitution, confirmation mismatch, and recovery assertions. Do not add a library, alternate clear method, or new abstraction layer.</action>
  <verify>
    <automated>node --test test/transaction.test.js && node --check server.js</automated>
  </verify>
  <done>Fixture tests prove the delete-project-file path archives and verifies before mutation, rejects every incomplete or mismatched acceptance state without mutation, pins the source, and retains a verified recovery with actionable guidance on every non-success path; UI never enables clearing from client-only state (D-01 through D-05).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Run direct sacrificial-device acceptance and recovery UAT</name>
  <files>data/clear-acceptance.json, test/transaction.test.js</files>
  <action>Run the opt-in direct UAT against one sacrificial OP-Z in Content Mode using only the local Node API and direct filesystem checks. For the exact delete-project-file method, capture the verified archive, safely eject, reconnect the same device, invoke the guarded clear, then verify the exact post-reconnect empty-slot representation, rejection behavior, playback/state sanity, and recovery by restoring the retained archive. Persist the bounded acceptance record only when every required outcome matches the exact project hash and device identity; otherwise leave automatic clearing disabled and retain the recovery instructions. The current environment has no OP-Z, so complete and verify all fixture coverage locally, then report hardware acceptance as pending without substituting `opzdisk/` evidence (D-01, D-02, D-05).</action>
  <verify>
    <automated>OPZ_HARDWARE_UAT=1 node --test --test-name-pattern='automatic clear sacrificial-device UAT' test/transaction.test.js</automated>
    <human-check>With a sacrificial OP-Z mounted in Content Mode, confirm the exact delete-project-file method ejects/reconnects, produces the expected empty slot, leaves the verified recovery archive intact, and restores the original project successfully. If no device is mounted, the opt-in test must skip or report pending and `clearEnabled` must remain false.</human-check>
  </verify>
  <what-built>Direct device acceptance evidence for the one configured clear method, or a fixture-only pending state when no OP-Z is available.</what-built>
  <how-to-verify>Run the opt-in Node test with a sacrificial OP-Z in Content Mode and inspect the API/filesystem evidence; do not use browser or screen control.</how-to-verify>
  <resume-signal>Resume after the direct UAT has either persisted a complete acceptance record or explicitly confirmed that hardware acceptance remains pending.</resume-signal>
  <done>Either a complete exact-method acceptance record is persisted and automatic clearing becomes eligible only for that method, or—when hardware is absent or any outcome is uncertain—the gate remains false, no fixture evidence is promoted to hardware evidence, and the retained recovery path is documented by the API result (D-02, D-05).</done>
</task>

</tasks>

<verification>
Run `node --test test/transaction.test.js` and `node --check server.js`. Run the opt-in hardware UAT only with a sacrificial OP-Z in Content Mode; direct API/filesystem evidence is required, and no browser or screen control is part of acceptance. Confirm the default no-device state keeps automatic clearing disabled and manual-free guidance intact.
</verification>

<success_criteria>
CLEAR-01, CLEAR-02, and CLEAR-03 are satisfied without weakening existing archive, recovery, source-pinning, or mutation-boundary checks; absent hardware leaves the implementation fixture-proven but automatic clearing disabled.
</success_criteria>

<output>
Create `.planning/phases/06-hardware-gated-automatic-clearing/06-01-SUMMARY.md` when done.
</output>

## Source Audit

| Source | ID | Coverage |
|---|---|---|
| GOAL | — | Task 1 + Task 2: gated automatic archive-and-free with retained recovery |
| REQ | CLEAR-01 | Task 1 gate; Task 2 records exact fixture/device acceptance |
| REQ | CLEAR-02 | Task 1 archive-first clear and post-reconnect confirmation |
| REQ | CLEAR-03 | Task 1/2 recovery receipt and explicit failure instructions |
| CONTEXT | D-01..D-05 | Task actions and acceptance criteria cite every locked decision |
