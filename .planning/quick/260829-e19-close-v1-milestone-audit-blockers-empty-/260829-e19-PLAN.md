---
quick_id: 260829-e19
status: complete
mode: quick-full
must_haves:
  truths:
    - "A verified archive can restore into an explicitly selected empty slot on the same reviewed source without inventing a backup."
    - "Automatic clear remains pending until the app observes device absence and a later same-device empty-slot reconnect."
    - "Pending split archives expose provenance, acceptance status, and a browser path that records all five observed hardware outcomes."
  artifacts:
    - "server.js"
    - "app/index.html"
    - "test/transaction.test.js"
  key_links:
    - "scanSlots empty target evidence -> /api/restore validation -> verified write/readback"
    - "/api/clear-slot pending result -> /api/state disconnect/reconnect reconciliation -> browser guidance"
    - "archiveShelfData split evidence -> acceptance checklist -> /api/split/acceptance -> restore eligibility"
---

# Close v1 Milestone Audit Blockers

## Task 1: Restore safely into an empty slot

**Files:** `server.js`, `app/index.html`, `test/transaction.test.js`

**Action:** Reuse the existing root/source identity proof for empty slots, accept a reviewed `null` target fingerprint only when the selected project file is absent, skip the nonexistent overwrite backup, preserve rollback-to-empty semantics, and update the restore copy/result.

**Verify:** A focused integration test restores a verified archive into an empty slot, proves exact readback, and proves no fake recovery archive was created.

**Done:** Empty and occupied targets both use explicit, source-bound restore contracts and stale target changes fail closed.

## Task 2: Require a reconnect boundary for automatic clear

**Files:** `server.js`, `app/index.html`, `test/transaction.test.js`

**Action:** Return a retained-recovery pending state after deletion. Reconcile it through `/api/state`: first observe device absence, then require the same source identity and an absent slot file on a later reconnect before reporting success. Surface the pending/confirmed/recovery guidance in the browser.

**Verify:** The clear integration test proves same-mount absence is pending, an observed disconnect is required, a same-device empty reconnect confirms success, and the recovery remains verified.

**Done:** No clear request reports `cleared: true` before reconnect confirmation.

## Task 3: Wire split acceptance through the product

**Files:** `server.js`, `app/index.html`, `test/transaction.test.js`

**Action:** Project split provenance and acceptance state to the shelf, add a native five-item observation checklist that calls the existing acceptance API, and serialize split confirmation through the shared mutation guard.

**Verify:** Static/UI and API integration checks prove the route has a browser consumer, all outcomes are explicit, accepted status refreshes, provenance renders, and split confirmation is mutation-serialized.

**Done:** The confirmed-half → synthesis → hardware acceptance → restore-eligible path is connected end to end.
